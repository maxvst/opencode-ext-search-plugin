- Добавить обрабоку ошибок из-за которых работа плагина невозможна или проблемы к конфигурацией opencode.json (плагин сообщает о пробеме через тултип и если ошибка фатальна, прекращает работу)

- Привести глоссарий и код в согласованное состояние

- Привести базовый сценарий и код в согласованное состояние

- Расширить глоссарий и привести его с кодом в согласованное состояние

- Описать реализованные сценарии и привести их в согласованное состояние с кодом.

- запускать внешний поиск при поиске промежуточных директориях от открытой директории до директории команды включая обе данные директории.

- включить поиск по внешним директориям при поиске во всех промежуточных директориях между директорией проекта и директорией команды.

- Принудительно производить поиск по всем внешним директориям и те, в которых соответствия не найдены, вычеркивать из подсказки.

- Вычеркивать из подсказки те директории, результаты по которым полностью поместились в подсказке.



# В планах

## Обработка ошибок

### Отображение toast ошибок

Реализуй новый функционал

# Новые сценарии
Плагин должен показать toast через opencode api c описанием возникшей проблемой в следующих случаях:
- не найдена утилита rg
- ошибка парсинга opencode.json
- не найден opencode.json, инициализирующий данный модуль
- не найдейна директория из списка directories в opencode.json
- пустой список внешних директорий
- другие события из-за которых работа модуля невозможна или ограничена

# Тестирование
Реализуй интеграционные тесты, проверяющие каждый из описанных сценариев, для этого
- создай mock объект OpenCode, который сэмулирует реальное поведение OpenCode (если в исходных кодах OpenCode найдешь образцовые примеры мока OpenCode, можешь полностью или частично скопировать их в проект, но прописывать импорты в директорию исходников opencode запрещено)
- возможно потребуется переписать работу модуля с файловой системой, чтобы добавить дополнительную мокируемую прослойку и замокировать ее.


